# UI Rules

## コンポーネント
shadcn/uiのコンポーネントを優先的に使う。独自CSSは最小限にする。

## スタイリング
Tailwind CSSのユーティリティクラスのみを使う。カスタムCSSファイルの追加は原則禁止。

## 画面IDの表示
開発中の画面には、コンポーネント冒頭コメントに対応する画面ID(例: HR-EMP-001)を必ず記載する。

## エラー・空状態
全ての一覧画面はローディング状態・空状態・エラー状態の3つを必ず実装する。

## アクセシビリティ
フォーム要素には必ずlabelを付与する。
